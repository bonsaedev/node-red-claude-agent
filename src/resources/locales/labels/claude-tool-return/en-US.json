{
  "label": "claude tool return",
  "configs": {
    "name": "Name",
    "format": "Return format",
    "errorPort": "Error port",
    "completePort": "Complete port",
    "statusPort": "Status port"
  },
  "options": {
    "format": {
      "text": "Text (a plain text block)",
      "json": "JSON (machine-readable structured content)"
    }
  },
  "input": {
    "label": "Return",
    "description": "The flow's answer to a Claude tool call. The value rides msg.output.payload; set msg.output.isError to return an error to the model."
  }
}
