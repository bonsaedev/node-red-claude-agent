{
  "label": "claude tool return",
  "configs": {
    "name": {
      "label": "Name",
      "description": "An optional label shown on the node in the editor."
    },
    "format": {
      "label": "Return format",
      "description": "Chooses whether msg.payload is returned to the model as a plain text block or as machine-readable JSON."
    },
    "errorPort": {
      "label": "Error port",
      "description": "Adds an error output port; when the node throws, the error is sent there instead of halting the flow."
    },
    "completePort": {
      "label": "Complete port",
      "description": "Adds a complete output port that emits after the node finishes handling a message."
    },
    "statusPort": {
      "label": "Status port",
      "description": "Adds a status output port that emits the node's status changes as messages."
    }
  },
  "options": {
    "format": {
      "text": "Text (a plain text block)",
      "json": "JSON (machine-readable structured content)"
    }
  },
  "input": {
    "label": "Return",
    "description": "The flow's answer to a Claude tool call. The value rides msg.payload; set msg.isError to return an error to the model."
  }
}
